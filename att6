#include <stdio.h>
#include <locale.h>

int main(){ 
    setlocale(LC_ALL, "Portuguese");
    
    double area;
	double pi = 3.14;
    double raio;
    
    printf("Digite o valor do raio do círculo: \n");
    scanf("%lf", &raio);
    
    area = (raio * raio) * pi;
    printf("A área do círculo é:  %.2lf \n", area);
    
      return 0;
}

#include <stdio.h>
#include <locale.h>

int main() {
    setlocale(LC_ALL, "Portuguese");
    
    int media1;
    int media2;
    int soma;
    float somamed;
    
    media1 = (7+8+9) /3;
    media2 = (4+5+6) /3;
    soma = media1 + media2;
    
    printf("A soma das duas médias é: %d \n", soma);
    
    somamed = (media1 + media2) / 2;
    printf("A média das médias é: %f \n", somamed);
    
    
    
    return 0;
}

#include <stdio.h>
#include <locale.h>

int main(){ 
    setlocale(LC_ALL, "Portuguese");
    
    float cent;
    float resultado;
    
    printf("Digite em centímetros o numero a ser convertido em polegadas: \n");
    scanf("%f", &cent);
    
    resultado = cent / 2.54;
    printf("Seu número em polegadas é: %.2f \n", resultado);
    
   return 0;
}
